<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.TabJsonApp" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Splash -->
    <style name="Theme.TabJsonApp.Splash" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/ic_launcher_foreground</item>
    </style>
</resources>
